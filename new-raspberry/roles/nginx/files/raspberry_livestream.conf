
server {
  listen 80 default_server;
  server_name _;

  ##
  # RTMP setings
  ##

  location / {
    rtmp_stat all;
    rtmp_stat_stylesheet stat.xsl;
  }

  location /control {
    rtmp_control all;
  }

  location /stat.xsl {
    root /home/pi/raspberry_livestream/;
  }

}
